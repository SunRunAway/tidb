name: LGTM label
on:
  pull_request_review:
    types: [submitted]
env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: andymckay/labeler@1.0.2
        if: >
         !contains(github.event.issue.labels.*.name, 'status/LGT1') && 
         !contains(github.event.issue.labels.*.name, 'status/LGT2') && 
         (contains(github.event.review.body, 'LGTM') || 
         contains(github.event.review.body, 'lgtm') || 
         contains(github.event.review.body, 'Lgtm'))
        with:
          add-labels: 'status/LGT1'
      - uses: andymckay/labeler@1.0.2
        if: >
         contains(github.event.issue.labels.*.name, 'status/LGT1') && 
         !contains(github.event.issue.labels.*.name, 'status/LGT2') && 
         (contains(github.event.review.body, 'LGTM') || 
         contains(github.event.review.body, 'lgtm') || 
         contains(github.event.review.body, 'Lgtm'))
        env:
          add-labels: 'status/LGT2'
          remove-labels: 'status/LGT1'
